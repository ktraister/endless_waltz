services:
  random:
    build:
      context: random
    ports:
      - "8090:8090"

  mongo:
    image: mongo
    restart: always
    ports:
      - "27017:27017"

  server:
    build:
      context: server
    ports:
      - "6000:6000"
