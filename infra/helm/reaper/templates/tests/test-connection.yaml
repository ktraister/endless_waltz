apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "reaper.fullname" . }}-test-connection"
  labels:
    {{- include "reaper.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "reaper.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
