# Default values for reaper.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

reaper:
  name: ew-reaper
  image:
    repository: ghcr.io/ktraister/ew_reaper
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: "8a92f57dcdaf561be196c11f8b48d66b102e97d7"

entropy:
  name: ew-entropy
  image:
    repository: ghcr.io/ktraister/ew_entropy
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: "6e912119197ff273ae5d5e0ac66ae1550529b471"

imagePullSecrets: 
  - name: ghcrcred

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: 
  privileged: true
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 12345

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

#my BS
mongouri: "mongodb://mongo-nodeport-svc.default.svc.cluster.local:32000"
mongouser: adminuser
mongopass: password123
