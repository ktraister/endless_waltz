apiVersion: v1
kind: Service
metadata:
  name: {{ include "random.fullname" . }}
  labels:
    {{- include "random.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "random.selectorLabels" . | nindent 4 }}
