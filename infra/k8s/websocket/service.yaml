apiVersion: v1
kind: Service
metadata:
  name: ew-websocket
  labels:
    app: ew-websocket
spec:
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
      name: http
  selector:
    name: ew-websocket
